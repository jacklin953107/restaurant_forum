<div class="container">
  <div class="row">
    <% @restaurants.each do |restaurant| %>
        <div class="col-sm-6 col-md-4">
          <div class="restaurant-item">
            <%= File.exist?('public'+image_path(restaurant.image)) %>
            <%= File.directory?('public'+image_path(restaurant.image)) %>
            <%= restaurant.image? %>
            <!--<% if restaurant.image?  %>-->
                <!--<% for img_id in 0..7 %>-->
                    <!--<% if image_path(restaurant.image).include?('/'+img_id.to_s+'.jpg') %>-->
                        <!--<%= image_tag('/seed_img/'+img_id.to_s+'.jpg', width:'300px', class: "img-responsive center-block") %>-->
                        <%= show_img(restaurant.image)%>
                    <!--<% end %>-->
                <!--<% end %>-->
                <!--<%= image_tag restaurant.image, width:'300px', class: "img-responsive center-block" %>-->
            <!--<% else %>-->
                <!--<div style="width:300px;height:180px">-->
                  <!--<span class="glyphicon glyphicon-picture"></span>-->
                <!--</div>-->
            <!--<% end  %>-->
            <div class="caption">
              <h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3>
              <p><%= restaurant.name %></p>
              <p><%= truncate(restaurant.description, length: 80) %></p>
            </div>
          </div>
        </div>
    <% end %>
    <div class="text-center">
      <%= paginate @restaurants %>
    </div>
  </div>
</div>